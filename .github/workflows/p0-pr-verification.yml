name: StayBid PR verification

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  verify:
    name: Verify frontend
    runs-on: ubuntu-24.04
    timeout-minutes: 30

    steps:
      - name: Check out pull request
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm ci --ignore-scripts --no-audit --no-fund

      - name: Install PostgreSQL 16 binaries
        run: |
          sudo apt-get update
          sudo apt-get install --yes postgresql-16

      - name: Check pull request whitespace
        run: git diff --check "${{ github.event.pull_request.base.sha }}" "${{ github.event.pull_request.head.sha }}"

      - name: Verify pricing-engine schema contract
        if: ${{ hashFiles('tests/pricing-engine-schema-contract.test.js') != '' }}
        run: node tests/pricing-engine-schema-contract.test.js

      - name: Verify Price Spine occupancy contract
        run: node tests/price-spine-occupancy.test.js

      - name: Type-check
        run: npx tsc --noEmit

      - name: Build production bundle
        run: npm run build

      - name: Run security regression suite
        run: npm run test:security

      - name: Run Voice AI foundation suite (SB-01)
        run: node tests/voice/voice-ai.test.js

      - name: Run Voice AI interaction-shell suite (SB-02)
        run: node tests/voice/voice-ux.test.js

      - name: Type-check Voice gateway (SB-04)
        run: npx tsc -p server/voice-gateway/tsconfig.json --noEmit

      - name: Run Voice AI provider/client suite (SB-04)
        run: node tests/voice/voice-provider.test.js

      - name: Run Voice AI session-broker suite (SB-04)
        run: node tests/voice/voice-session-broker.test.js

      - name: Run Voice AI gateway suite (SB-04)
        run: node tests/voice/voice-gateway.test.js

      - name: Run Voice AI gateway security suite (SB-04)
        run: node tests/voice/voice-gateway-security.test.js

      - name: Run concurrency regression suite
        run: npm run test:concurrency
