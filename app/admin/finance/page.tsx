import StubPage from "@/components/admin/stub-page";
export default function Page() {
  return (
    <StubPage
      title="Commission & Finance"
      icon="📊"
      description="Commission ledger per hotel (GMV × override %), payout queue (pending / processed / paid) with bank transfer trigger, and monthly financial reports with CSV/PDF export."
    />
  );
}
