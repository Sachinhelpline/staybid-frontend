import StubPage from "@/components/admin/stub-page";
export default function Page() {
  return (
    <StubPage
      title="Video Verification"
      icon="🎥"
      description="3-tab view (Pending / Submitted / Complaints) with AdaptiveVideoPlayer, AI trust scores, fraud flag inspection, and verdict actions (approve / refund / partial / replacement / deny)."
    />
  );
}
