import StubPage from "@/components/admin/stub-page";
export default function Page() {
  return (
    <StubPage
      title="Complaints"
      icon="🚨"
      description="Complaint queue (bid / booking / verification / general) with priority filters, AI-suggested resolutions, internal notes thread, and auto-actions (Razorpay refund trigger, hotel notification escalation)."
    />
  );
}
