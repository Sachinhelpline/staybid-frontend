// /circle/model2 — Model 2: Multi-City Inventory Bundle (v346).
//
// Canonical route for the merged Model 2 (was Model 3 pre-buy + Model 4 B2B
// exchange). Phase 1 (naming) points it at the existing pre-buy browse+buy flow
// — the ss3 Step 1–3 "access city calendar → buy released rooms → build". The
// unified released-dates calendar + multi-select basket + selling inventory
// land in the later Model-2 phases. `/circle/model3` + `/circle/model4` stay
// live (rebranded) and cross-link here.
export { default } from "../model3/page";
